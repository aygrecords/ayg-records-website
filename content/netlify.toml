[build]
  publish = "."

[[redirects]]
  from = "/admin/*"
  to = "/admin/:splat"
  status = 200
[build]
  publish = "."

# Decap / Netlify CMS Single Page Application Routing
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200

# Optional: Netlify Identity / CMS OAuth handling
[[redirects]]
  from = "/api/v1/*"
  to = "/.netlify/functions/:splat"
  status = 200
